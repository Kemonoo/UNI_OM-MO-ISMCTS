# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# --- Virtual environments ---
.venv/
venv/
env/

# --- Experiment outputs (large + fully regenerable; see README) ---
# The main ablation JSON is ~400 MB. Never commit it - regenerate it instead.
data/*.json
logs/

# Result figures (data/*.png) ARE tracked on purpose: they are the thesis
# figures and small. To stop tracking them, uncomment the next line:
# data/*.png

# --- Editor / OS / local config ---
.vscode/
.idea/
*.local

